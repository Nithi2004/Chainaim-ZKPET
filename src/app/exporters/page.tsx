// src/app/exporters/page.tsx
"use client"; // Ensure this is a client component

import Exporters from '../components/Exporters';

const ExportersPage = () => {
  return <Exporters />;
};

export default ExportersPage;
